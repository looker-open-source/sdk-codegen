name: release-please
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@5450bfcc4a83666e0caeb4dbe2da1a98fca7067d
        with:
          command: manifest
          token: ${{ secrets.LOS_AUTO_BOT_RP_TOKEN }}
